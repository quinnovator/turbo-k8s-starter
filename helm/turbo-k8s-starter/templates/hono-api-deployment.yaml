apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "turbo-k8s-starter.fullname" . }}-hono-api
  labels:
    {{- include "turbo-k8s-starter.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.honoApi.replicaCount }}
  selector:
    matchLabels:
      {{- include "turbo-k8s-starter.selectorLabels" . | nindent 6 }}
      app: hono-api
  template:
    metadata:
      labels:
        {{- include "turbo-k8s-starter.labels" . | nindent 8 }}
        app: hono-api
    spec:
      {{- with .Values.honoApi.imagePullSecrets | default .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: hono-api
          image: "{{ .Values.honoApi.image.repository }}:{{ .Values.honoApi.image.tag }}"
          imagePullPolicy: {{ .Values.honoApi.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.honoApi.service.port }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.honoApi.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.honoApi.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.honoApi.resources | nindent 12 }}