{{- if .Values.goApi.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "turbo-k8s-starter.fullname" . }}-go-api
  labels:
    {{- include "turbo-k8s-starter.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "turbo-k8s-starter.fullname" . }}-go-api
  minReplicas: {{ .Values.goApi.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.goApi.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.goApi.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.goApi.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
{{- end }}